<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title></title>
</head>
<body>
    <div>
        <!--标准语法 ifelse-->
        {{if age < 10}}
            <p>age < 10</p>
        {{else if age < 20}}
            <p>age < 20</p>
        {{else}}
            <p>age >= 20</p>
        {{/if}}
        <!--原始语法 ifelse-->
        <% if (age < 10) { %>
            <p>age < 10</p>
        <% } else if (age < 20) { %>
            <p>age < 20</p>
        <% } else { %>
            <p>age >= 20</p>
        <%}%>
    </div>
</body>
</html>